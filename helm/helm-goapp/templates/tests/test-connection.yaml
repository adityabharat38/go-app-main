apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-goapp.fullname" . }}-test-connection"
  labels:
    {{- include "helm-goapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-goapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
